% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_embedding.R
\name{compute_xgeo_embedding}
\alias{compute_xgeo_embedding}
\title{Compute and attach a platform embedding}
\usage{
compute_xgeo_embedding(
  state,
  source = c("explanations", "point_meta", "points"),
  method = c("pca", "umap"),
  dims = 2L,
  name = NULL,
  ...
)
}
\arguments{
\item{state}{An \code{xgeo_state} object.}

\item{source}{Source matrix used to build the embedding. One of
\code{"explanations"}, \code{"point_meta"}, or \code{"points"}.}

\item{method}{Embedding backend. \code{"pca"} is always available; \code{"umap"}
requires \code{uwot}.}

\item{dims}{Number of output dimensions.}

\item{name}{Optional embedding name. Defaults to \verb{<method>_<source>}.}

\item{...}{Passed to backend-specific implementations.}
}
\value{
The updated \code{xgeo_state}.
}
\description{
Compute and attach a platform embedding
}
\examples{
state <- as_xgeo_state(
  data.frame(
    point_id = rep(paste0("p", 1:4), each = 2),
    feature = rep(c("f1", "f2"), times = 4),
    x = c(0, 0, 1, 1, 0, 0, 1, 1),
    y = c(0, 0, 0, 0, 1, 1, 1, 1),
    value = c(0.2, 0.7, 0.4, 0.1, 0.8, 0.6, 0.5, 0.3)
  ),
  point_id_col = "point_id",
  feature_col = "feature"
)
state <- compute_xgeo_embedding(state, method = "pca", source = "explanations", dims = 2)

names(state$attributes$embeddings$items)
}
