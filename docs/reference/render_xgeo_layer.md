# Render one `xgeo_layer`

Render one `xgeo_layer`

## Usage

``` r
render_xgeo_layer(layer, scene, ...)
```

## Arguments

- layer:

  Layer object to render.

- scene:

  Parent scene.

- ...:

  Reserved for future extension methods.

## Value

Invisibly returns `NULL`.
