# Coerce inputs to `xgeo_data`

Coerce inputs to `xgeo_data`

## Usage

``` r
as_xgeo_data(x, ...)
```

## Arguments

- x:

  An object to coerce.

- ...:

  Passed to method-specific implementations.

## Value

A `xgeo_data` object.
