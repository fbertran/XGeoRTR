# Get attributes from an `xgeo_state`

Get attributes from an `xgeo_state`

## Usage

``` r
xgeo_attributes(state)
```

## Arguments

- state:

  An `xgeo_state` object.

## Value

The `attributes` field.
