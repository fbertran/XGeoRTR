# Get selection from an `xgeo_state`

Get selection from an `xgeo_state`

## Usage

``` r
xgeo_selection(state)
```

## Arguments

- state:

  An `xgeo_state` object.

## Value

The `selection` field.
